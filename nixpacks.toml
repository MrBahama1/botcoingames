[phases.setup]
nixPkgs = ["nodejs_20", "python312", "python312Packages.pip"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "npm install -g @bankr/cli"
]

[start]
cmd = "python main.py --no-browser --port $PORT"
